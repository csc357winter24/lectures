#include <stdio.h>

/* NOTE: main's arguments are the command line arguments,
 *       but if we're not going to use them, we should
 *       define them as void instead. */
int main(void) {
    printf("Goodbye, world!\n");

    return 0;
}
