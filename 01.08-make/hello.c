#include <stdio.h>

/* NOTE: The arguments to main represent the command line arguments to the
 *       program; they can be declared as void if they are unused. */
int main(void) {
    printf("Hello, world!\n");

    return 0;
}
